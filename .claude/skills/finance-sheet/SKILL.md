---
name: finance-sheet
description: "구글 시트 '금융 자산 관리'(자산·부채·순자산 추적) 조회·입력·관리. 월말 잔액 입력, 입출금 기록, 적금 만기/납입 처리, 순자산·목표 계산 질문 시 사용. 증시 일일 리뷰(/blog-kr, /blog-us)나 종목 신호와는 무관."
---

# 금융 자산 관리 시트 관리 규약

사용자(도담아빠)의 개인 자산·부채·순자산을 월별로 추적하는 구글 시트를 조회/입력/관리하는 스킬. 증시 리뷰 시트(`증시 리뷰_YYMMDD`)와는 **완전히 별개**의 시트다.

## 시트 식별
- 이름: **`금융 자산 관리`** (끝에 `_` 붙은 `금융 자산 관리_`는 **구버전/백업 — 건드리지 말 것**)
- key: `1Jnm55njxzB8NxY0LJAlrum8WPMHuD-EdeMlpHMQfHrY`

## 접속 방법
프로젝트 기존 인증(`sheet_auth.get_credentials` → token.pickle) 재사용:

```python
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
import gspread
from sheet_auth import get_credentials
gc = gspread.authorize(get_credentials())
sh = gc.open_by_key('1Jnm55njxzB8NxY0LJAlrum8WPMHuD-EdeMlpHMQfHrY')
ws = sh.worksheet('저축')   # 탭 이름으로 접근
```
- 실행 디렉터리: `C:\Users\dongh\Desktop\주식\AI agent`
- 읽기: `ws.get_all_values()`. 쓰기: `ws.update_acell('C5', 값)` 또는 `ws.append_row([...])`
- **쓰기 전 항상 현재 값을 먼저 읽어 사용자에게 확인받고 진행** (개인 자산 데이터 — 임의 덮어쓰기 금지)

## 탭 구조 (행 1~2가 헤더, 데이터는 월별 1행)

### `저축` — 예적금·연금 월별 잔액
| 컬럼 | 내용 |
|------|------|
| A 월 | `YYYY-MM` |
| B 청년도약계좌 / C 일반적금 / D 주택청약 / E IRP | 각 상품 **월말 잔액** |
| F 합계 | B~E 합 |
| G 입출금(추가입금/출금) | 그달의 순수 납입(+)/인출(−). **손익 보정용** |
| H 월 손익 = (F 이번달 − F 지난달 − G) | 자동 |
| I 월 수익률 / J 누적 수익률 | 자동 |

### `주식` — 주식 계좌 평가액
| 컬럼 | 내용 |
|------|------|
| A 월 / B 주식 평가액 | 월말 총평가액(예수금 포함) |
| C 입출금(추가입금/출금) | **`입출금 이력` 탭 합계로 자동 계산** — 직접 입력 금지 |
| D 월 손익 = (B 이번달 − B 지난달 − C) / E 월수익률 / F 누적수익률 | 자동 |

### `입출금 이력` — 주식 계좌 현금 입출금 로그
| A 날짜 | B 금액 | C 메모 |
- 입금 = `+숫자`, 출금 = `−숫자`. 발생 시 **한 줄씩 append**
- `주식` 탭 C열이 이 탭 합계를 월별로 자동 집계
- 예: 월급 입금 `+2,500,000`, 생활비 출금 `−1,500,000`, 적금 납입 출금 `−1,000,000`

### `대출` — 부채 월별 잔액
- 서울시이차보전대출 / 예적금담보대출 각각 잔액·상환원금·이자, 합계·누적 자동

### `기타자산` — 인벤토리 (보증금 등)
- 분류 / 자산명 / 취득일 / 취득가 / 현재가치 / 메모. 전세보증금 등

### `종합` — 월별 순자산 대시보드 (대부분 자동)
| 운용자산(KB+IRP) | 예적금 | 자산합계 | 대출잔액 | 순자산 | 월변동·변동률·누적 |
- `예적금` = 저축 탭 합계, `운용자산` = 주식+IRP, `순자산` = 자산합계 − 대출

### `목표 계산기` — 시나리오 (전부 자동)
- 현재 순자산 기준, 목표 순자산 × 목표 시점 → 필요 연환산 수익률 매트릭스

---

## 핵심 원칙: 손익 vs 계좌 간 이동 (이게 자주 까먹는 부분)

이 시트의 모든 `월 손익`은 **`(이번달 잔액 − 지난달 잔액 − 입출금)`** 로 계산된다.
따라서 **계좌 사이를 옮긴 돈(이동)은 손익이 아니므로 반드시 입출금 컬럼으로 상쇄**해야 수익률이 왜곡되지 않는다. 순수하게 시장에서 불어난/줄어든 것만 손익이다.

판단 기준:
- **외부에서 자산이 늘거나 줄었나?** (시장 평가손익) → 입출금 0, 잔액만 변동 → 손익으로 잡힘 ✔
- **내 계좌끼리 돈이 이동했나?** (납입·인출·만기) → 양쪽에 입출금 기록 → 손익 0, 순자산 불변 ✔

---

## 주요 작업 레시피

### ① 적금/예금 만기 → 주식 계좌 예수금으로 이동
가장 헷갈리는 케이스. **두 곳에 반대 방향으로 기록**한다:

1. **`저축` 탭** (빠져나가는 쪽)
   - 만기된 상품(예: C 일반적금) 잔액을 해당 월에 **0 또는 차감**
   - 같은 행 **G 입출금에 `−만기금액`** (인출) 기록
   - → 저축에서 손실로 안 잡히고 정상 인출로 처리

2. **`입출금 이력` 탭** (들어오는 쪽)
   - 한 줄 append: `날짜 | +만기금액 | "적금 만기"`
   - → `주식` C열에 자동 합산되어, 늘어난 평가액이 수익이 아닌 입금으로 처리

결과: `종합`에서 예적금 ↓ / 운용자산 ↑ 가 상쇄 → **순자산 불변(wash)**, 양쪽 월수익률 정상.
> ⚠️ ②만 하고 ①을 빠뜨리면 저축 탭에 그달 큰 손실로 찍힌다. 반드시 양쪽.
> 만기금을 주식이 아니라 **다른 적금/소비로** 보내면 ②(입출금 이력) 대신 그쪽 처리만.

### ② 적금 납입 (주식 계좌에서 적금으로)
사용자는 그동안 적금 납입을 **주식 계좌 출금**으로 처리해 옴(입출금 이력에 `−, 메모 "적금"`).
1. `입출금 이력`에 `−납입액, "적금"` append
2. `저축` 탭 해당 상품 잔액 증액 + G 입출금에 `+납입액`
→ 만기(①)는 이것의 **정확히 반대 방향**.

### ③ 월말 마감 입력 (매월 말 루틴)
1. `저축`: 각 상품 월말 잔액(B~E), 그달 순납입/인출(G) 입력
2. `주식`: 월말 평가액(B)만 입력 (C는 자동)
3. `입출금 이력`: 그달 입출금이 다 들어갔는지 확인
4. `대출`: 잔액·상환원금·이자 입력
5. `종합`·`목표 계산기`는 자동 — 값만 검증

### ④ 월급/생활비 등 일반 입출금
- `입출금 이력`에 한 줄 append (`+월급`, `−생활비/월세/교육비`)
- 주식 계좌 기준 현금 흐름만 기록. 저축/대출과 무관하면 거기 건드릴 필요 없음

---

## 주의사항
- **개인 민감 자산 데이터** — 외부 전송·커밋 금지, 쓰기 전 사용자 확인 필수
- 금액은 시트에서 천단위 콤마 표시이나 입력 시 숫자만(또는 `=` 수식). 기존 셀 서식 유지
- 수익률·합계·순자산 컬럼은 **수식 자동** — 직접 입력해 덮어쓰지 말 것
- `예수금 5~10% 상시 유지, 최대 매수 95%` 운용 룰은 별개(메모리 user-cash-management). 이 시트는 기록용이지 매수 판단용이 아님
