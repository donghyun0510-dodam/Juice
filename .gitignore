# ── 민감 파일 (절대 커밋 금지) ──
client_secret.json
token.pickle
credentials.json

# ── 런타임 상태 파일 (Actions commit-back으로 동기화) ──
# 의도적으로 tracked — .gitignore에 없어야 commit-back 가능

# ── 환경변수 ──
.env
.env.local

# ── 유틸리티/디버그 스크립트 (하드코딩 포함, 로컬 전용) ──
read_template.py
check_folder.py
find_folder.py
cleanup.py
check_semis.py
check_recovery.py
semis_out.txt

# ── 로그 ──
log_*.txt
*.log

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/

# ── IDE / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 스케줄러 임시 파일 ──
task_global.xml
task_global_utf16.xml
.claude/agents/signal-judge.md
sa_credentials.json
trendfollow-scouter-*.json
